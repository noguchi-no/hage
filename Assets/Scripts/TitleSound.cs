using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TitleSound : MonoBehaviour
{
    //bool isbgm = true; 
    //bool isNotbgm;
    //public AudioSource bgm;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {/*
        if(!isNotbgm){
            if(AdMobInters.adOpened){
                 bgm.Stop();
                 AdMobInters.adOpened = false;
            }
            isNotbgm = true;
            isbgm = false;
        }
        if(!isbgm){
            if(AdMobInters.adClosed){
                 bgm.Play();
                 AdMobInters.adClosed = false;
            }
            isbgm = true;
        }*/
    }
}
